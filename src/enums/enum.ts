export enum Boolean {
    True = 1,
    False = 0,
}

export enum Role {
    CareReceiver = "IAmFollowing",
    CareGiver = "MyFollowers",
}

export enum Status {
    Accepted = "accepted",
    Pending = "pending",
    Rejected = "rejected"
}

export enum InvitationType {
    MyFollower = 1,
    IAmFollowing = 2,
}


